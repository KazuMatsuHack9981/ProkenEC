<p>(secret) all user data</p>

<table>
  <tr>
    <th>username</th><th>email</th><th>password</th><th>admin</th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.email %></td>
    <td><%= user.password %></td>
    <td>
      <% if user.admin==true then %>
        true
      <% else %>
        false
      <% end %>
    </td>
  </tr>
  <% end %>
</table>
<%= link_to "戻る", homepage_index_path %>
